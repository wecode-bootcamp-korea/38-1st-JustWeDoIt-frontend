import React from 'react';
import './Input.scss';

const Input = label => {
  // const { name, koreanName } = label;
  // const [inPutSet, setInPutSet] = useState({ email: '', userName: '', pw: '' });
  // const saveInPutSet = e => {
  //   setInPutSet({ ...inPutSet, [e.target.name]: e.target.value });
  // };
  return (
    <div>
      <input type="text" />
    </div>
  );
};

export default Input;
